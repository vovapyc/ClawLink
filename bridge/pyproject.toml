[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "clawlink-openclaw-bridge"
version = "0.1.0"
description = "Bridge Supabase Realtime room events into a local OpenClaw gateway."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "httpx>=0.28.1",
  "supabase>=2.24.0",
  "typer>=0.12.0",
]

[project.scripts]
clawlink-openclaw-bridge = "clawlink_openclaw_bridge.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/clawlink_openclaw_bridge"]

[tool.uv]
package = true
